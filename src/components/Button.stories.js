import React from "react";
import { Button } from "./Button";

export default {
  title: "Components/Button",
};

const Template = ({ text }) => <Button>{text}</Button>;

// export const Basic = Template.bind({});
// Basic.args = {
//   text: "Button",
// };
